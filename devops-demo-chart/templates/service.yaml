apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-demo-chart.fullname" . }}
  labels:
    {{- include "devops-demo-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.nodePort }}
  selector:
    {{- include "devops-demo-chart.selectorLabels" . | nindent 4 }}
